<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | Sistem Arsip</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
  <style>
    body {
      display: flex;
      min-height: 100vh;
    }
    .sidebar {
      width: 280px;
      min-height: 100vh;
      background-color: #343a40;
      color: white;
    }
    .content {
      flex: 1;
      padding: 2rem;
    }
  </style>
</head>
<body>
  <div class="d-flex">
    <% if (session && session.userId) { %>
    <div class="sidebar p-3 d-flex flex-column">
      <h4><i class="bi bi-archive-fill"></i> Menu Utama</h4>
      <hr>
      <ul class="nav nav-pills flex-column mb-auto">
        <li class="nav-item">
          <a href="/" class="nav-link text-white">
            <i class="bi bi-speedometer2"></i> Dashboard
          </a>
        </li>
        <li>
          <a href="/config" class="nav-link text-white">
            <i class="bi bi-gear-wide-connected"></i> Manajemen Konfigurasi
          </a>
        </li>
        <li>
          <a href="/users" class="nav-link text-white">
            <i class="bi bi-people"></i> User Management
          </a>
        </li>
        <li>
          <a href="/logs" class="nav-link text-white">
            <i class="bi bi-clipboard-data"></i> Activity Logs
          </a>
        </li>
      </ul>
      <hr>
      <div class="dropdown">
        <a href="#" class="d-flex align-items-center text-white text-decoration-none dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
          <i class="bi bi-person-circle me-2"></i>
          <strong><%= session.userEmail %></strong>
        </a>
        <ul class="dropdown-menu dropdown-menu-dark text-small shadow">
          <li><a class="dropdown-item" href="/logout">Sign out</a></li>
        </ul>
      </div>
    </div>
    <% } %>

    <div class="content">
      <%- body %>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
